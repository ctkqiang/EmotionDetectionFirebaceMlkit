<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.firebase.samples.apps.Johnmelodyme">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applogo"
        android:label="@string/app_name"
        android:theme="@style/MaterialTheme"
        android:roundIcon="@mipmap/applogo"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name="com.google.firebase.samples.apps.Johnmelodyme.ChooserActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.google.firebase.samples.apps.Johnmelodyme.LivePreviewActivity" android:exported="true">
        </activity>

        <activity android:name="com.google.firebase.samples.apps.Johnmelodyme.StillImageActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>